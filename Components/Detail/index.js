import React from 'react'
import { View, Text } from 'react-native'

const index = ({navigation}) => {
    return (
        <View>
            <Text>Détails</Text>
        </View>
    )
}

export default index
